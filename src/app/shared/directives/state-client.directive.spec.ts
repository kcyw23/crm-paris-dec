import { StateClientDirective } from './state-client.directive';

describe('StateClientDirective', () => {
  it('should create an instance', () => {
    const directive = new StateClientDirective();
    expect(directive).toBeTruthy();
  });
});
