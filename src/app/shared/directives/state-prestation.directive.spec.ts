import { StatePrestationDirective } from './state-prestation.directive';

describe('StateDirective', () => {
  it('should create an instance', () => {
    const directive = new StatePrestationDirective();
    expect(directive).toBeTruthy();
  });
});
